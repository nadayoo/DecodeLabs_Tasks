# DecodeLabs_Chatbot
A Python terminal chatbot that responds to user input using a dictionary (hash map) of predefined responses. No machine learning, pure control flow logic. The key concepts are input sanitization, a while True loop, dictionary-based O(1) lookup, a fallback response, and a clean exit command.
